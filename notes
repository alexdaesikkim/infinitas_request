things to do:
1. make sure there's 4 additional fields (for now, until disabled can be integrated) per song, and deal with default unlocks
2. finish unlock panel first before moving the code over (if done right, should just be copy pastenotes)
